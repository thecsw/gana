name: Go
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go 1.18 🛠️
        uses: actions/setup-go@v1
        with:
          go-version: 1.18
        id: go
      - name: Get the code 😤
        uses: actions/checkout@v1
      - name: Test 🏃‍♀️
        run: go test -v ./...
